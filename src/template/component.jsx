import React from 'react'

export default class {{ capitalize name }} extends React.Component {
  render() {
    return <div className="{{ capitalize name }}">
      <h1>{{ capitalize name }} Component</h1>
    </div>
  }
}
